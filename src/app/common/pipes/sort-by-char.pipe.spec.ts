import { SortByCharPipe } from './sort-by-char.pipe';

describe('SortByCharPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCharPipe();
    expect(pipe).toBeTruthy();
  });
});
