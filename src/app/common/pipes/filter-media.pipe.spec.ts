import { FilterMediaPipe } from './filter-media.pipe';

describe('FilterMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
